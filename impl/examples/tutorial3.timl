fun add (a, b) = a + b

val add : int * int -- 1.0 --> int = fn (a, b) => a + b

fun add (a, b) return using 1.0 = a + b

fun add (a, b) return int using 1.0 = a + b

fun add (a, b) = a + b using 0.0
                       
